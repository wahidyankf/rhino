# A hook is a sequence, and without this a gate that failed would print its
# finding and let the commit through -- the failure mode a gate cannot have.
set -e

# The surface declares the order, and the order is the point: the safety gate
# runs before the formatter, so a staged leak is refused rather than tidied.
exec cargo run --quiet --bin rhino -- gate run --surface pre-commit
